using konoha.posix.*;

void main(String[] args)
{
	OUT << "[D-Script] [web_reconfigure] start" << EOL;
	usleep(100000);
	OUT << "[D-Script] [web_reconfigure] succesd" << EOL;
	OUT << "[D-Script] [web_reconfigure] end" << EOL;
}
