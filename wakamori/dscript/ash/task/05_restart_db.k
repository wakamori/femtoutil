using konoha.posix.*;

void main(String[] args)
{
	OUT << "[D-Script] [db_restart] start" << EOL;
	usleep(100000);
	OUT << "[D-Script] [db_restart] succesd" << EOL;
	OUT << "[D-Script] [db_restart] end" << EOL;
}
