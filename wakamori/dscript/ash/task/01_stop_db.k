using konoha.posix.*;

void main(String[] args)
{
	OUT << "[D-Script] [db_stop] start" << EOL;
	usleep(500000);
	OUT << "[D-Script] [db_stop] succesd" << EOL;
	OUT << "[D-Script] [db_stop] end" << EOL;
}
