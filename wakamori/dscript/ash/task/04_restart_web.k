using konoha.posix.*;

void main(String[] args)
{
	OUT << "[D-Script] [web_restart] start" << EOL;
	usleep(100000);
	OUT << "[D-Script] [web_restart] succesd" << EOL;
	OUT << "[D-Script] [web_restart] end" << EOL;
}
