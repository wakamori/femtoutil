using konoha.posix.*;

void main(String[] args)
{
	OUT << "[D-Script] [web_stop] start" << EOL;
	usleep(500000);
	OUT << "[D-Script] [web_stop] succesd" << EOL;
	OUT << "[D-Script] [web_stop] end" << EOL;
}
